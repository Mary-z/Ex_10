spark.eventLog.enabled           true
spark.eventLog.dir               /tmp/spark-events

spark.default.parallelism        8

spark.driver.memory              1g            # Память для драйвера

spark.executor.memory            1g            # Память для каждого воркера
#spark.executor.cores             1             # Количество ядер для каждого воркера

#spark.executor.instances         2            

spark.local.cleanup.enabled      true           # Включить очистку локальных файлов
